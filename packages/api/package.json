{
  "name": "@fastbuild/api",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./testing": {
      "types": "./src/testing/factories.ts",
      "default": "./src/testing/factories.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "biome:check": "biome check .",
    "biome:fix": "biome check --write .",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "test": "vitest run",
    "test:integration": "vitest run src/__tests__/integration",
    "test:unit": "vitest run src/__tests__/unit",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@fastbuild/auth": "workspace:*",
    "@fastbuild/db": "workspace:*",
    "@fastbuild/logger": "workspace:*",
    "@fastbuild/validators": "workspace:*",
    "@trpc/server": "catalog:",
    "superjson": "2.2.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@faker-js/faker": "^8.0.0",
    "@fastbuild/biome-config": "workspace:*",
    "@fastbuild/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^1.0.0"
  }
}
